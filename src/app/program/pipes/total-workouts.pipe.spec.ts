import { TotalWorkoutsPipe } from './total-workouts.pipe';

describe('TotalWorkoutsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalWorkoutsPipe();
    expect(pipe).toBeTruthy();
  });
});
