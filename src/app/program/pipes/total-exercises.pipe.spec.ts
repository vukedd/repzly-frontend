import { TotalExercisesPipe } from './total-exercises.pipe';

describe('TotalExercisesPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalExercisesPipe();
    expect(pipe).toBeTruthy();
  });
});
