import { TotalSetsPipe } from './total-sets.pipe';

describe('TotalSetsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalSetsPipe();
    expect(pipe).toBeTruthy();
  });
});
